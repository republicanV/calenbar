import { layerConditional } from 'embcalendar/helpers/layer-conditional';
import { module, test } from 'qunit';

module('Unit | Helper | layer conditional');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = layerConditional([42]);
  assert.ok(result);
});
